class MovieModel {
  final String name;
  final String imageUrl;
  final int route;

  MovieModel({this.name, this.imageUrl, this.route});

  // MovieModel.fromJson(movie) {
  //   name = movie.name ?? '';
  //   imageUrl = movie.imageUrl ?? '';
  //   route = movie.route ?? 0;
  // }
}