import 'package:flutter/material.dart';


class RadialMenuScreen extends StatelessWidget {
  static String routeName = '/radial-menu';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
